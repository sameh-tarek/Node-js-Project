<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Course</title>

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">

    <style>
        body {
            background-color: #f8f9fa;
        }

        .container {
            margin-top: 50px;
        }

        .card-header {
            background-color: #007bff;
            color: #fff;
            font-weight: bold;
        }

        .form-control:focus {
            border-color: #007bff;
            box-shadow: none;
        }

        button[type="submit"] {
            background-color: #007bff;
            color: #fff;
            font-weight: bold;
            border: none;
        }

        button[type="submit"]:hover {
            background-color: #0069d9;
        }

        h1,
        h2 {
            text-align: center;
        }

        .course-list {
            margin-top: 50px;
        }

        .course-list .card {
            margin-bottom: 20px;
        }
        
        .btn-primary.custom {
            background-color: #004085;
            color: #fff;
            padding: 10px 20px;
            margin-top: 20px;
        }
        
        .btn-primary.custom:hover {
            background-color: #0069d9;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">Add New Course</h5>
                    </div>
                    <div class="card-body">
                        <form action="/courses" method="post">
                            <div class="form-group">
                                <label for="name">Course Name</label>
                                <input type="text" class="form-control" id="name" name="name" required>
                            </div>
                            <div class="form-group">
                                <label for="code">Course Code</label>
                                <input type="text" class="form-control" id="code" name="code" required>
                            </div>
                            <div class="form-group">
                                <label for="department">Department</label>
                                <select class="form-control" id="department" name="department" required>
                                    <option value="">Select Department</option>
                                    {{#each departments}}
                                    <option value="{{_id}}">{{department_name}}</option>
                                    {{/each}}
                                </select>
                            </div>
                            <div class="form-group">
                                <label for="require_course">Required Pre-Course</label>
                                <select class="form-control" id="require_course" name="require_course">
                                    <option value="">No Required Pre-Courses</option>
                                    {{#each courses}}
                                    <option value="{{course_code}}">{{course_name}}</option>
                                    {{/each}}
                                </select>
                            </div>

                            <div class="form-group">
                                <label for="doctor_id">Doctor ID</label>
                                <input type="text" class="form-control" id="doctor_id" name="doctor_id" required>
                            </div>
                            
                            <button type="submit" class="btn btn-block">Add Course</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
        <div class="row">

            <div class="col-md-12 d-flex justify-content-center">
              <a href="/attedance" class="btn btn-primary custom">Back to Attendance</a>
            </div>
            
        </div>
    </div>
    
    <!-- Bootstrap JS -->
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
</body>
</html>