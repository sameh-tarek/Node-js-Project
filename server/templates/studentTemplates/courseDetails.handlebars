<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        a{
            text-decoration: none;
        }
        .contain {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin: 20px;
            padding: 20px;
            border: 1px solid rgb(197, 192, 192);
            border-radius: 10px;
            box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.2);
        }
        .contain:hover {
            background-color: #f5f5f5;
        }
    </style>
</head>
<body>
    <h1>{{subject.course_name}}</h1>

    {{#each materials}}
        <div class="contain">
            <h2>
                <a href="/student/studentCourses/{{course_code}}/{{file_name}}" >{{file_name}}</a>
            </h2>
        </div>
    {{/each}}
</div>
</body>
</html>